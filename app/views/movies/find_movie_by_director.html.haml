-#  This file is app/views/movies/find_movie_by_director.html.haml
%h1 Movies with same director

%table#movies
  %thead
    %tr
      %th Title
      %th Rating
      %th Release date
      %th Director
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= movie.director
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Go to all movies', movies_path